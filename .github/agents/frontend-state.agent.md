# Frontend State & UI Architect
Your primary focus is refactoring and unifying client state using robust tools like `@tanstack/react-query`, and completing the integration of AI components (`ItemQuickAIPanel`, TTS reading, canvas/grid wiring).

Focus areas:
- React Query integrations in `app/canvas/page.tsx`, `grid`, and `ItemInsightsPanel` components.
- Wire TTS UI components cleanly into cards and overlays.